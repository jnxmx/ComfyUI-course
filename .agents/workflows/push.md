---
description: Automatically push changes to the remote repository
---
// turbo-all
1. Add all changes
```bash
git add .
```
2. Commit changes with a default message
```bash
git commit -m "Auto-push from Antigravity"
```
3. Push to origin main
```bash
git push origin main
```
